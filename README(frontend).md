# Маркетплейс автозапчастей

Добро пожаловать в **Маркетплейс автозапчастей**! Это платформа электронной коммерции для покупки и продажи автозапчастей с поддержкой ролей покупателя, продавца и администратора.

## Основные функции
- 🛒 **Покупатели**: Просмотр каталога с фильтрами, управление корзиной, оформление заказов, отслеживание доставки.
- 📦 **Продавцы**: Управление товарами, обработка заказов, управление доставкой, аналитика продаж.
- ⚙️ **Администраторы**: Модерация товаров, управление пользователями, аналитика платформы, настройка параметров (например, стоимость доставки).
- 🔐 **Авторизация**: Регистрация и вход для всех ролей с проверкой секретного ключа для администраторов.

## Установка

1. Установите `http-server` для локального сервера:
   ```bash
   npm install -g http-server
   ```

2. Перейдите в корневую папку проекта:
   ```bash
   cd marketplace-autoparts
   ```

3. Запустите сервер:
   ```bash
   http-server -c-1
   ```

4. Откройте браузер и перейдите по адресу:
   ```
   http://localhost:8080/auth/index.html
   ```

5. Используйте тестовые учетные записи из `auth/data/users.json` для входа.

## Структура проекта

```
marketplace-autoparts/
├── auth/                  # Модуль авторизации
│   ├── data/
│   │   └── users.json     # Данные пользователей
│   ├── index.html         # Страница входа/регистрации
│   ├── style.css          # Стили
│   └── script.js          # Логика авторизации
├── dashboard/
│   ├── buyer/             # Модуль покупателя
│   │   ├── cart/          # Корзина
│   │   ├── checkout/      # Оформление заказа
│   │   ├── data/          # Данные товаров и заказов
│   │   ├── order/         # История заказов
│   │   ├── shipping/      # Отслеживание доставки
│   │   ├── index.html     # Главная страница
│   │   ├── style.css      # Стили
│   │   └── script.js      # Логика
│   ├── seller/            # Модуль продавца
│   │   ├── order/         # Управление заказами
│   │   ├── process-order/ # Обработка заказов
│   │   ├── shipping/      # Управление доставкой
│   │   ├── index.html     # Главная страница
│   │   ├── style.css      # Стили
│   │   └── script.js      # Логика
│   └── admin/             # Модуль администратора
│       ├── analytics/     # Аналитика платформы
│       ├── orders/        # Управление заказами
│       ├── products/      # Модерация товаров
│       ├── settings/      # Настройки платформы
│       ├── users/         # Управление пользователями
│       ├── index.html     # Главная страница
│       ├── style.css      # Стили
│       └── script.js      # Логика
├── docs/                  # Документация модулей
│   ├── buyer(frontend).md
│   ├── seller(frontend).md
│   ├── admin(frontend).md
│   ├── PROJECT_PLAN.md
│   ├── README(frontend).md
└── Images/                # Изображения товаров
```

## Модули

### Модуль авторизации (`auth`)
- **Функции**: Регистрация, вход, переключение ролей, проверка секретного ключа для админов.
- **Подробности**: См. [auth(frontend).md](docs/auth(frontend).md).

### Модуль покупателя (`dashboard/buyer`)
- **Функции**: Просмотр каталога, фильтры (цена, бренд, категория), поиск по названию/VIN, управление корзиной, оформление заказов, отслеживание доставки.
- **Подробности**: См. [buyer(frontend).md](docs/buyer(frontend).md).

### Модуль продавца (`dashboard/seller`)
- **Функции**: Управление товарами, обработка заказов, управление доставкой, экспорт данных в Excel, аналитика продаж.
- **Подробности**: См. [seller(frontend).md](docs/seller(frontend).md).

### Модуль администратора (`dashboard/admin`)
- **Функции**: Модерация товаров, управление пользователями (блокировка, сброс пароля, изменение ролей), управление заказами, аналитика (графики, экспорт данных), настройки платформы.
- **Подробности**: См. [admin(frontend).md](docs/admin(frontend).md).

## Технологии и зависимости

- **Frontend**: HTML5, CSS3, JavaScript (ES6+), Fetch API, Flexbox, Grid.
- **Хранение данных**: `localStorage`, JSON-файлы (`users.json`, `products.json`, `orders.json`).
- **Библиотеки**:
  ```html
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.9.1/chart.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
  ```

## Тестовые данные

**Пользователи** (`auth/data/users.json`):
```json
[
    {
        "id": 1,
        "username": "buyer1",
        "email": "buyer1@example.com",
        "password": "password123",
        "role": "buyer"
    },
    {
        "id": 2,
        "username": "admin1",
        "email": "admin1@example.com",
        "password": "admin123",
        "role": "admin",
        "secretKey": "ADMKEY2025"
    },
    {
        "id": 3,
        "username": "seller1",
        "email": "seller1@example.com",
        "password": "seller123",
        "role": "seller"
    }
]
```

**Товары** (`dashboard/buyer/data/products.json`):
```json
[
    {
        "id": 1,
        "name": "Тормозные колодки Toyota Corolla",
        "price": 12500,
        "images": ["../../Images/brake_pads.jpg"],
        "specifications": {
            "material": "Керамика",
            "compatibility": "Toyota Corolla 2015-2023",
            "brand": "Toyota",
            "category": "brakes"
        }
    },
    {
        "id": 2,
        "name": "Масляный фильтр Bosch",
        "price": 3500,
        "images": ["../../Images/oil_filter.jpg"],
        "specifications": {
            "material": "Синтетика",
            "compatibility": "Универсальный",
            "brand": "Bosch",
            "category": "engine"
        }
    }
]
```

**Заказы** (`dashboard/buyer/data/orders.json`):
```json
[
    {
        "orderNumber": "ORD001",
        "userId": 1,
        "items": [
            {
                "productId": 1,
                "quantity": 2,
                "price": 12500
            }
        ],
        "total": 25000,
        "status": "pending",
        "trackingNumber": null,
        "date": "2025-05-10"
    }
]
```

## Тестирование

Для проверки функциональности используйте тестовые данные и следующие сценарии:
- **Авторизация**: Вход и регистрация для всех ролей, проверка секретного ключа для админов.
- **Покупатель**: Поиск товаров, добавление в корзину, оформление заказа, отслеживание доставки.
- **Продавец**: Добавление/редактирование товаров, обработка заказов, экспорт в Excel.
- **Администратор**: Модерация товаров, управление пользователями, изменение статусов заказов, просмотр аналитики.

Рекомендуемые инструменты для тестирования:
```bash
npm install
npm test
```
Добавьте юнит-тесты (Jest) и e2e-тесты (Cypress) для полной проверки.

## Дополнительные замечания
- **Безопасность**: Пароли хранятся в открытом виде для демонстрации. В продакшене используйте хеширование (например, bcrypt).
- **Локализация**: Интерфейс на русском языке, возможно добавление поддержки других языков через i18n.
- **Ограничения**: Текущая версия работает с локальными данными. Для масштабирования требуется серверная часть (Node.js, Django и т.д.).
- **Расширяемость**: Структура позволяет добавлять новые модули и функции.

## Лицензия
Проект распространяется под лицензией MIT. См. [LICENSE](LICENSE).